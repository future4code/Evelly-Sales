import React from "react";
import App from "../../App";

const passaCards = (props) => {

    id={''}
    nomeUsuario={''}
    idade={''}
    fotoPost={''}
    bio={''}

}

export default function UseAppContainer (pageLimit: Number){
    const [fotos, setFotos]

    function fetchPhotos (page: Number) {
        const virtualPage = ((page -1)* pageLimit) <= 0
        ? 0
        :((page -1)* pageLimit)

       
    }
    
    return { UseAppContainer

    }
}